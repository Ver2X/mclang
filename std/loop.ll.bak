define dso_local i32 @loop()() {
entry:
  % n = alloca i32, align 4 % sum = alloca i32, align 4 % i = alloca i32,
    align 4 store i32 50, i32 * % n, align 4 store i32 0, i32 * % sum,
    align 4 store i32 0, i32 * % i,
    align 4 br label %
        while
            .cond

        while.cond : %
                     0 = load i32,
    i32 * % i, align 4 % 1 = load i32, i32 * % n,
    align 4 % cmp = icmp slt i32 % 0, % 1 br i1 % cmp, label % while.body,
    label %
        while
            .end

        while.body : %
                     2 = load i32,
    i32 * % sum, align 4 % 3 = load i32, i32 * % i,
    align 4 % add = add nsw i32 % 2, % 3 store i32 % add, i32 * % sum,
    align 4 % 4 = load i32, i32 * % i, align 4 % add1 = add nsw i32 % 4,
    1 store i32 % add1, i32 * % i,
    align 4 br label %
        while
            .cond

        while.end : %
                    5 = load i32,
    i32 * % sum, align 4 ret i32 % 5
}

declare void @llvm.dbg.declare(metadata, metadata, metadata) #1
